name: test

type: php:5.6
build:
    flavor: symfony

relationships:
    database: "mysql:mysql"

web:
    document_root: "/web"
    passthru: "/app.php"

disk: 2048

mounts:
    "/app/cache": "shared:files/cache"
    "/app/logs": "shared:files/logs"

hooks:
    build: |
      rm web/app_dev.php
    deploy: |
      app/console --env=prod cache:clear
      app/console --env=prod doctrine:schema:update --force -n

runtime:
  extensions:
    - name: blackfire
      configuration:
        server_id: "f0aba685-caaa-4f75-af60-92c0bac53344"
        server_token: "b226642844e29b86168a504902f09f1fa28098c7b7919fc5202fc859e6d7dea5"
